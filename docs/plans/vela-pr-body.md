Vela's homepage now collects Jenn's projects with search, category filters, status labels, site links and individual project notes. Five live projects include actual site screenshots; paused projects retain their descriptions without dead visit links.

The strategy-jenn-os AI lab is migrated from its recorded deployment commit into Jenn's password-protected workspace. Existing client workspaces, methods and downloads remain intact. Authentication fails closed without a signing secret, and private lab observations remain server-only. Dependencies are patched; npm audit reports no vulnerabilities.

Validation: production build, ESLint, browser navigation/filter/auth/geometry checks, router boundary tests and independent visual review. Broad-region geometry passes at four viewport widths; exhaustive glyph-level geometry remains unproven. Doldol is retained, Cinque remains paused, and old Vercel projects are not deleted by this change.
