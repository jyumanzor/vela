# Vela portfolio consolidation

Owner: Jenn. Requested September 20, 2026. Public portfolio plus private workspaces.

Visual thesis: retain Vela's forest-night palette, serif titles, and a restrained gold constellation; use a compact editorial project directory with generous reading space.
Content plan: Vela identity and purpose, searchable project index, individual project notes, existing methods and downloads, private workspace entry. Each project carries purpose, status, contents, design choices, and a meaningful destination.
Interaction thesis: project rows reveal a directional affordance; search and category controls narrow the directory; project detail pages provide progressive disclosure instead of an endless homepage.

References: existing Vela homepage owns palette and type; Vela client access surfaces own restrained spacing; strategy-jenn-os at production commit 7a0c7e8 owns the AI use-case lab. Existing Vela showcase is the anti-reference for stale live links and generic browser mockups. No new external visual direction is being invented.

Canonical target: existing vela-io Vercel project and Vela repository site/ app. Vela is the brand. Do not purchase a domain or delete either old project during this integration. Compare and verify before any later retirement.

Preserve Doldol permanently unless Jenn explicitly changes that instruction. Cinque remains paused. Other paused apps stay paused; public notes must label their status. No private client graphs, internal lab observations, credentials, or unreviewed research records become public portfolio copy.

Integration: migrate the recorded strategy-jenn-os production lab into Jenn's authenticated workspace, retaining its routing logic. Keep existing Vela methods, downloads, client workspaces, and authentication. The source working tree for strategy now holds different intern content; import the deployed commit, not that working tree.

Geometry: 390, 768, 1440 and 1920 px viewports; page scale 1; max width 1120; 24px mobile/40px desktop gutters; title 56-88px on directory, 40-64px on detail; body 16-18px; section heads 24-36px; 44px interactive targets; content-driven rows; no overlapping regions. A fixed global header owns its first 72px. Search/filter controls wrap on mobile. Project detail uses two columns only above 900px. The existing third monospace font remains in legacy routes, but new portfolio uses serif + sans only.

Organization: compact searchable list on / and /projects; subpages at /projects/[slug]. /showcase and /access/jenn redirect to the catalog. Lab and client workspaces are private; public case studies describe their purpose without embedding private data.

Evidence: live API inventory and HTTP checks September 20: six live external projects; Doldol, Cinque, Carina paused. Mainline contains later download/navigation changes absent from June production, so preserve mainline and add only missing portfolio material. Operational inheritance check reports pre-existing unrelated agent/control warnings; no OS controls modified.
