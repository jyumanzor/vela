# Matt Music Archive

Private Next.js seed project for organizing recordings, sessions, and collaborators.

## Run

```bash
npm install
npm run dev
```

The local dev server uses port `3005`.

## Privacy

The checked-in data is sample-only. Keep real audio files and private notes local until the sharing rule is explicit.
